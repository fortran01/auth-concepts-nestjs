<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - NestJS Auth Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container my-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card border-danger">
                    <div class="card-header bg-danger text-white">
                        <h3 class="mb-0">Error</h3>
                    </div>
                    <div class="card-body">
                        <h4 class="card-title">{{message}}</h4>
                        
                        {{#if error}}
                            <div class="alert alert-danger mt-3">
                                <strong>Error details:</strong> {{error.error}}
                            </div>
                            
                            {{#if error.stack}}
                                <div class="mt-4">
                                    <h5>Stack Trace</h5>
                                    <pre class="bg-light p-3 text-danger">{{error.stack}}</pre>
                                </div>
                            {{/if}}
                        {{/if}}
                        
                        <div class="mt-4">
                            <a href="/" class="btn btn-outline-secondary">Return to Home</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 