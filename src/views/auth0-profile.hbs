<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card">
            <div class="card-header bg-secondary text-white">
                <h3 class="mb-0">Auth0 Profile</h3>
            </div>
            <div class="card-body">
                <div class="text-center mb-4">
                    <img src="{{user.picture}}" alt="Profile Picture" class="rounded-circle img-thumbnail" style="width: 100px; height: 100px;">
                    <h4 class="mt-2">{{user.name}}</h4>
                    <p class="text-muted">{{user.email}}</p>
                </div>
                
                <div class="alert alert-info">
                    <p><strong>About Auth0 Universal Login:</strong></p>
                    <p>You were authenticated using Auth0's Universal Login. Key points:</p>
                    <ul>
                        <li>Authentication was handled by Auth0's hosted login page</li>
                        <li>OpenID Connect (OIDC) protocol was used for authentication</li>
                        <li>Your profile information was retrieved using your access token</li>
                        <li>Session management is handled by the application</li>
                    </ul>
                </div>
                
                <div class="mt-4">
                    <h5>Profile Information</h5>
                    <div class="bg-light p-3 rounded">
                        <pre class="mb-0"><code>{{json user}}</code></pre>
                    </div>
                </div>
                
                <div class="d-grid gap-2 mt-4">
                    <a href="/auth0/logout" class="btn btn-secondary">Logout</a>
                </div>
            </div>
        </div>
    </div>
</div> 