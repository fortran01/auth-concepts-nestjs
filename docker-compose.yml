version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: nestjs_auth_redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  redis_data: 